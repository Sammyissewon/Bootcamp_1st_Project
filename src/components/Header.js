import React from 'react';
import './Header.css';

const Header = () => {
  return (
    <header>
      <div className="header">가입하기</div>
      <div className='h2'>이미 사용 중인 계정이 있다면 로그인하세요.</div>
    </header>
  );
};

export default Header;
